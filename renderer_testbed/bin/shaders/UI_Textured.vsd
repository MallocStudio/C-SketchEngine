#version 450

layout ( location = 0 ) in vec4 in_vert; // x,y are positions, z,w are uvs

uniform mat4 projection_view;

out vec2 _uv;

void main () {
    gl_Position = projection_view * vec4(in_vert.xy, 0, 1);
    _uv = in_vert.zw;
}