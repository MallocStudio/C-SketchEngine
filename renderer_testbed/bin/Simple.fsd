#version 450

in vec4 _Colour;
in vec2 _TexCoord;

uniform sampler2D texture0; // diffuse

out vec4 FragColour;

void main()
{
	// FragColour = vec4(Colour, 1);
	//

	//FragColour = _Colour;
	FragColour = texture(texture0, _TexCoord);
}