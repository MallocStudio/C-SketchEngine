#version 330 core

out vec4 FragColour;

void main()
{
    // sense we're using the depth buffer we don't need any rendering here
    FragColour = vec4(gl_FragCoord.z, gl_FragCoord.z, gl_FragCoord.z, 1.0);
}