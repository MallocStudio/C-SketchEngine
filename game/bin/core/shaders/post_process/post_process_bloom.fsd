out vec3 FragColour;

uniform sampler2D bloom_texture;

void main () {
    vec3 result = texture(texture_id, _TexCoord).rgb; // the scene
    result += texture(bloom_texture, _TexCoord).rgb;  // bright colours
    FragColour = result;
}