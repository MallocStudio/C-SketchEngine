{
    "tasks": [
        {
            "type": "shell",
            "label": "Build sketchengine",
            "windows": {
                "command": "${workspaceFolder}/sketchengine/build.bat"
            },
            "options": {
                "cwd": "${workspaceFolder}/sketchengine"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build",
            "detail": "compile and build sketchengine"
        },
    ],
    "version": "2.0.0"
}